# 14. 데이터 저장과 성능 계약

**문서:** Haggle Engine Architecture v1.0.2 — 데이터 & 성능
**범위:** Redis Hot State, PostgreSQL Cold Storage, Engine Core 연산 예산, 시스템 성능 목표, 확장 단계
**관련 문서:** [00_INDEX.md](./00_INDEX.md) | [01_아키텍처_개요.md](./01_아키텍처_개요.md) | [13_LLM_비용.md](./13_LLM_비용.md)

---

## 1. 데이터 저장

Haggle Engine은 **Hot/Cold 이중 저장소** 구조를 사용한다. 실시간 협상 상태는 Redis에, 영구 기록은 PostgreSQL에 보관한다.

### 1.1 Redis (Hot State)

모든 키는 TTL이 설정되어 있으며, 세션 종료 후 자동 정리된다.

```
# 세션 데이터 (TTL: 세션 마감 + 1시간)
session:{id}:state           → 세션 상태
session:{id}:last_offer      → 마지막 제안
session:{id}:utility         → 마지막 UtilityResult

# 오케스트레이션 (TTL: 세션 그룹 마감)
orch:{group_id}:topology     → 토폴로지
orch:{group_id}:sessions     → 활성 세션 목록
orch:{group_id}:batna        → 현재 BATNA
orch:{group_id}:rankings     → 세션 순위

# 대기 Intent 인덱스
waiting:buyer:{category}     → Set[intent_id]
waiting:seller:{category}    → Set[intent_id]
intent:{intent_id}           → WaitingIntent (TTL: expires_at)

# 에스컬레이션 제어
escalation:{session_id}:count → 에스컬레이션 횟수 (최대 5)
pressure:{session_id}:count   → 크로스 프레셔 횟수 (최대 2)

# v1.0.1: Agent Stats 캐시
stats:{user_id}:params       → Stats→파라미터 변환 결과 (TTL: 전략 만료 시)
```

### 1.2 PostgreSQL (Cold Storage)

영구 저장이 필요한 전략, 대기 인텐트, 에스컬레이션 로그를 관리한다.

```sql
CREATE TABLE master_strategies (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  category VARCHAR(100),
  strategy JSONB NOT NULL,
  agent_stats JSONB,                    -- v1.0.1: 원본 AgentStats
  stats_preset VARCHAR(20),             -- v1.0.1: 사용된 프리셋
  llm_calls_count INT DEFAULT 1,
  created_at TIMESTAMPTZ,
  expires_at TIMESTAMPTZ
);

CREATE TABLE waiting_intents (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  role VARCHAR(10) NOT NULL,
  category VARCHAR(100) NOT NULL,
  keywords TEXT[],
  strategy_id UUID REFERENCES master_strategies(id),
  min_u_total FLOAT DEFAULT 0.3,
  max_active_sessions INT DEFAULT 5,
  status VARCHAR(20) DEFAULT 'ACTIVE',
  created_at TIMESTAMPTZ NOT NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  fulfilled_at TIMESTAMPTZ
);

CREATE TABLE escalation_log (
  id UUID PRIMARY KEY,
  session_id UUID NOT NULL,
  trigger_reason VARCHAR(50),
  llm_input_summary TEXT,
  strategy_changes JSONB,
  llm_mode VARCHAR(20),            -- "reasoning_on" | "reasoning_off" | "rule_based"
  cost_usd FLOAT,
  created_at TIMESTAMPTZ
);

CREATE INDEX idx_waiting_active
  ON waiting_intents(category, status)
  WHERE status = 'ACTIVE';
```

---

## 2. 성능 계약

### 2.1 Engine Core 연산 예산

`computeUtility()` 한 번 호출의 세부 연산 예산이다. 순수 산술 연산만 포함되며, I/O는 일절 없다.

| 연산 | 예산 | 근거 |
|------|------|------|
| 입력 검증 | < 100μs | 경계 검사만 |
| V_p 계산 | < 50μs | ln() 2회 + 산술 |
| V_t 계산 | < 10μs | pow() 1회 |
| V_r 계산 | < 5μs | 곱셈 1회 |
| V_s 계산 | < 10μs | 나눗셈 1회 + 산술 |
| 경쟁 조정 | < 20μs | ln() 1회 + 곱셈 (선택적) |
| 가중합 | < 5μs | 곱셈 4회 + 덧셈 3회 |
| **총계** | **< 200μs** | **순수 산술, I/O 없음** |

### 2.2 Stats→파라미터 변환 예산 (v1.0.1)

Agent Stats에서 협상 파라미터로의 변환 연산 예산이다.

| 연산 | 예산 | 근거 |
|------|------|------|
| 스탯 검증 | < 50μs | 범위 + 합계 검사 |
| 파라미터 변환 | < 100μs | 산술 20회 + 정규화 |
| **총계** | **< 150μs** | **순수 산술, I/O 없음** |

> **참고:** Stats→파라미터 변환은 전략 생성 시 1회만 수행. 매 라운드에는 불필요.

### 2.3 시스템 레벨 성능

전체 시스템의 성능 목표이다.

| 메트릭 | 목표 |
|--------|------|
| 라운드 P95 (6-Stage Pipeline) | < 3s (LLM 3회 포함) |
| Init P95 (Stage 0a+0b) | < 5s (LLM 2회 포함) |
| Referee 검증 (Stage 4) | < 50ms (Engine Core, LLM 없음) |
| Batch Evaluate 200개 | < 50ms |
| 서버 인스턴스당 처리량 | 10,000 req/sec |

### 2.4 확장 단계

유저 수 증가에 따른 아키텍처 확장 계획이다.

| Phase | 유저 수 | 아키텍처 |
|-------|---------|----------|
| MVP | 1K | 단일 서버, Redis Standalone |
| Growth | 10K | 로드밸런서, Redis Cluster |
| Scale | 100K | Kubernetes, Multi-region |

---

*이전 문서: [13_LLM_비용.md](./13_LLM_비용.md) | 다음 문서: [15_적합성_테스트.md](./15_적합성_테스트.md) | [00_INDEX.md로 돌아가기](./00_INDEX.md)*
