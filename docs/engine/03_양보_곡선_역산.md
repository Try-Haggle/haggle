# 03. 양보 곡선과 Offer 역산

**문서:** Haggle Engine Architecture v1.0.2 — 양보 곡선과 Offer 역산
**범위:** Faratin 양보 곡선, 동적 베타, AC_next, 동적 마감, Offer Inverter
**관련 문서:** [00_INDEX.md](./00_INDEX.md) | [02_효용_함수.md](./02_효용_함수.md) | [04_상대방_모델.md](./04_상대방_모델.md)

---

## 1. Faratin 양보 곡선

### 1.1 가격 공간 공식 (v1.0.0)

$$P(t) = P_{start} + (P_{limit} - P_{start}) \times \left(\frac{t}{T}\right)^{1/\beta}$$

| 변수 | 정의 |
|------|------|
| P_start | 초기 제안가 |
| P_limit | 마지노선 |
| t | 현재 시간 (0~T) |
| T | 총 협상 시간 |
| β | 양보 속도. v1.0.1: AgentStats의 tenacity로 도출. |

### 1.2 β의 의미

| β | 곡선 | 스타일 | 페르소나 |
|---|------|--------|---------|
| 0.5 | 볼록 (급양보) | 초반 많이, 후반 적게 | Dolphin (관계적) |
| 1.0 | 선형 | 균등 양보 | Fox (균형잡힌) |
| 3.0 | 오목 (고집) | 초반 적게, 후반 많이 | Tiger (공격적) |

### 1.3 효용 공간 양보 곡선 (v1.0.1)

기존 Faratin은 가격 공간에서 양보한다. 효용 공간 모드(`use_utility_space=true`)를 켜면, 4차원 효용을 반영한 역제안 가격을 산출한다.

```
1. U_target(t) = U_aspiration + (U_threshold - U_aspiration) × (t/T)^(1/β)
2. v_p_target = (U_target - w_t·V_t - w_r·V_r - w_s·V_s) / w_p
3. price = invertVp(v_p_target, p_target, p_limit)
```

**장점:** 시간·리스크·관계 효용이 높을 때 가격 양보를 줄이고, 반대의 경우 가격으로 보상한다.

`use_utility_space` 기본값은 `false` — 기존 price-space Faratin 유지.

### 1.4 invertVp (computeVp의 역함수)

```
Buyer:  p = p_limit - (p_limit - p_target + 1)^v_p + 1
Seller: p = p_limit + (p_target - p_limit + 1)^v_p - 1
```

### 1.5 후반 라운드 공격 완화 (v1.0.1)

Rapport > 60인 에이전트는 `late_round_aggression_modifier = 0.8`이 적용된다.

```
기본: P_counter = P(t)
Rapport > 60:
  concession_amount = P(t) - P(t-1)
  P_counter = P(t-1) + concession_amount × late_round_aggression_modifier
```

마지노선 방향으로의 이동폭이 20% 줄어든다. 더 관대한 역제안으로 관계를 보존한다.

---

## 2. 동적 베타 (v1.0.1 + v1.0.2 확장)

### 2.1 v1.0.1 수식 (보존)

$$\beta_{competition} = \beta_{base} \times (1 + \kappa \cdot \ln(n_{competitors} + 1))$$
$$\beta_{dynamic} = \beta_{competition} \times (1 + \lambda \cdot opponent\_concession\_rate)$$
$$\beta_{final} = \text{clamp}(\beta_{dynamic}, 0.1, 10.0)$$

| 파라미터 | 기본값 | 역할 |
|----------|--------|------|
| κ (kappa) | 0.5 | 경쟁 민감도. 경쟁자 많을수록 β↑ |
| λ (lambda) | 0.3 | 상대 반응 민감도. 상대 양보 중이면 β↑ |

`opponent_concession_rate`는 OpponentModel의 EMA 양보율 (04_상대방_모델.md 참조).

경쟁/상대 데이터가 없으면 β_base 그대로 반환 (하위 호환).

### 2.2 v1.0.2 추가 — Faratin 4.2.1 자원 의존 조정

`listing_deadline`이 제공될 때, 엔진은 추가로 계산합니다:

```
t_max_dynamic = mu * |N|^2 / max(avg_thread_length, 1)
t_max_calendar = max(1, days_left * avg_rounds_per_day)
t_max_final = min(t_max_calendar, t_max_dynamic)
```

이것이 beta_effective에 영향:
```
ratio = t_max_dynamic / mu_baseline
beta_faratin = beta_base / max(ratio, 0.1)
```

### 2.3 두 수식 조율

두 수식 모두 실행됩니다. 엔진은 더 보수적인(낮은 beta = 더 Boulware) 값을 사용합니다:

```typescript
function computeEffectiveBeta(params: EffectiveBetaParams): number {
  // v1.0.1 동적 베타 (경쟁 + 상대 EMA)
  const betaDynamic = computeDynamicBeta({
    beta_base: params.beta_base,
    n_competitors: params.n_competitors,
    opponent_concession_rate: params.opponent_concession_rate,
    kappa: params.kappa,
    lambda: params.lambda,
  });

  // v1.0.2 Faratin 자원 의존 베타
  if (params.listing_deadline && params.mu_baseline) {
    const betaFaratin = computeFaratinBeta(params);
    // 더 보수적 (Boulware 방향) 값 사용
    return Math.min(betaDynamic, betaFaratin);
  }

  return betaDynamic;
}
```

---

## 3. AC_next 수락 조건 (v1.0.1)

역제안 가격을 계산한 후, 상대의 현재 제안이 이미 그보다 좋은지 확인한다.

```
Buyer:  incoming_price ≤ counter_price → ACCEPT
Seller: incoming_price ≥ counter_price → ACCEPT
```

```
shouldAcceptNext(incoming, counter, p_target, p_limit) → boolean
```

COUNTER→ACCEPT 업그레이드 시 `ac_next_triggered=true` 플래그를 RoundResult에 표시하여 투명성을 확보한다. 불필요한 라운드를 제거하여 비용과 시간을 절약한다.

---

## 4. 동적 마감 (v1.0.2 신규)

### 4.1 목적

판매자 마감일(listing_deadline)과 경쟁 상황을 통합하여, 양보 곡선의 마감(T)을 동적으로 조정합니다.

### 4.2 수식

```
t_max_dynamic = mu * |N|^2 / max(avg_thread_length, 1)
t_max_calendar = max(1, days_left * avg_rounds_per_day)
t_max_final = min(t_max_calendar, t_max_dynamic)
```

### 4.3 파라미터

```typescript
interface DynamicDeadlineParams {
  listing_deadline?: string;          // ISO 날짜, 판매자 설정 (기본: 14일, 최소: 7일)
  mu_baseline: number;               // 기준 협상 시간 (기본: 10.0)
  n_active_sessions: number;         // 현재 병렬 협상 수
  avg_thread_length: number;         // 활성 스레드 평균 라운드 수
  avg_rounds_per_day: number;        // 일당 추정 라운드 수 (기본: 5)
}
```

### 4.4 t_max 통합

```
t_max_final = min(t_max_calendar, t_max_dynamic)

이 값이 반영되는 곳:
1. 양보 곡선: U_target(t)에서 T로 t_max_final 사용
2. V_t 계산: t_deadline이 t_max_final로 오버라이드 가능
3. 세션 타임아웃: t_max_final에서 세션 자동 만료

의미:
  경쟁자 많음 → t_max_dynamic 증가 → 여유 생김 → Boulware
  경쟁자 빠져나감 → t_max_dynamic 감소 → 압박 증가 → Conceder
  판매자 마감일 임박 → t_max_calendar 감소 → 절대적 상한
  둘 중 더 빠른 마감이 항상 적용됨.
```

---

## 5. Offer Inverter (v1.0.2 신규)

### 5.1 목적

Decision Maker가 COUNTER를 반환하면 엔진은 실제 역제안을 생성해야 합니다. v1.0.1은 가격 공간(단일 Term)에서만 역산 가능했습니다. v1.0.2는 다중 Term 역산을 추가합니다.

### 5.2 파이프라인

```
1. Faratin 효용 공간 곡선으로 U_target(t) 계산
2. Term을 분리: NEGOTIABLE vs INFORMATIONAL
3. INFORMATIONAL Term의 고정 기여분 계산
4. 남은 효용 = U_target - informational_utility
5. 남은 효용을 NEGOTIABLE Term들에 분배
6. 각 NEGOTIABLE Term에 대해 V_j → x_j 역산
7. 완전한 offer 반환 {term_id: value}
```

### 5.3 역산 전략

`inversion_strategy` 파라미터로 두 전략 중 선택:

**PROPORTIONAL (기본, Opponent Model 불필요):**
```
현재 NEGOTIABLE Term 효용의 비율을 유지합니다.
U_target 달성을 위해 모두 비례적으로 스케일링.

scale = negotiable_target / current_negotiable_u
각 NEGOTIABLE Term j에 대해:
  v_target_j = min(1.0, v_current_j * scale)
  x_j = invert_V_j(v_target_j, domain_j)
```

**OPPONENT_AWARE (Opponent Model 필요):**
```
추정된 상대 가중치를 활용하여 양보를 분배합니다.
상대가 덜 중시하는 Term에서 더 많이 양보합니다.
상대가 중시하는 Term에서는 덜 양보합니다.

각 NEGOTIABLE Term j에 대해:
  상대 가중치 낮은 Term → 더 양보 (우리에게 저렴한 양보)
  상대 가중치 높은 Term → 덜 양보 (우리에게 비싼 양보)
```

### 5.4 V_j 역함수

**선형 V_j (구매자, lower_is_better):**
```
x_j = max_j - v_j * (max_j - min_j)
```

**선형 V_j (판매자, higher_is_better):**
```
x_j = min_j + v_j * (max_j - min_j)
```

**로그 V_p (기존 invertVp, 보존):**
```
P_effective = P_limit - exp(v_p * ln(P_limit - P_target + 1)) + 1
```

### 5.5 인터페이스

```typescript
interface OfferInverterParams {
  u_target: number;                    // 목표 총 효용
  terms: Term[];                       // Term 정의
  current_values: Record<string, number>;
  inversion_strategy: 'PROPORTIONAL' | 'OPPONENT_AWARE';
  estimated_opponent_weights?: Record<string, number>;  // OPPONENT_AWARE용
}

interface InvertedOffer {
  values: Record<string, number>;      // 각 Term의 제안값
  achieved_utility: number;            // 역산된 offer의 실제 U
}

function invertOffer(params: OfferInverterParams): InvertedOffer;
```

### 5.6 단일 Term 하위 호환

`term_space`가 null (기존 4차원 모드)일 때, 기존 `computeUtilitySpaceCounterOffer()` 함수가 활성 경로로 유지됩니다. V_p만 `invertVp()`를 사용해 역산하며, 이는 NEGOTIABLE Term 1개(가격)와 INFORMATIONAL Term 3개(시간, 리스크, 관계)로 구성된 Offer Inverter와 동등합니다.

---

## 6. 파라미터 계층

### Layer 2: 단일 스레드 전략 (보존 + 확장)

| 파라미터 | 타입 | 설명 | 출처 |
|----------|------|------|------|
| `RV` / `u_threshold` | float [0,1] | Reservation Value | Strategy Skill |
| `U_start` / `u_aspiration` | float [RV,1] | 초기 제안 효용 목표 | Strategy Skill |
| `beta` | float > 0 | 양보 형태 | Strategy Skill / AgentStats |
| `T` / `t_deadline` | int/float | 마감 | Strategy Skill |
| `use_utility_space` | bool | 효용 vs 가격 공간 곡선 | Strategy Skill |
| `inversion_strategy` | enum | PROPORTIONAL / OPPONENT_AWARE | Strategy Skill |

### Layer 3: 역제안 생성 (v1.0.2 신규)

| 파라미터 | 타입 | 설명 | 출처 |
|----------|------|------|------|
| `move_history` | MoveRecord[] | 세션 이동 분류 이력 | 엔진 (자동) |
| `last_move_type` | string | 상대의 마지막 이동 (6종) | 엔진 (자동) |
| `epsilon` | float | 이동 분류 임계값 | Strategy Skill |

### Layer 5: 다자간 & 경쟁 (v1.0.2 확장)

| 파라미터 | 타입 | 설명 | 출처 |
|----------|------|------|------|
| `n_active_sessions` | int | 현재 병렬 협상 수 | 시스템 |
| `avg_thread_length` | float | 평균 스레드 라운드 수 | 시스템 |
| `mu_baseline` | float | 기준 협상 시간 (기본: 10) | Strategy Skill |
| `listing_deadline` | date | 판매자 마감일 (기본: 14일) | Listing Skill |
| `t_max_final` | int | 통합 마감 (자동) | 엔진 (자동) |

---

*이전 문서: [02_효용_함수.md](./02_효용_함수.md) | 다음 문서: [04_상대방_모델.md](./04_상대방_모델.md)*
