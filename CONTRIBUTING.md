# Haggle 개발 참여 규칙

Haggle의 모든 사람과 개발 에이전트는 코드 변경 전에 주간 작업 그래프를 확인한다. 이 규칙의 목적은
누가 무엇을 맡았는지, 누구에게 무엇을 리뷰받아야 하는지, 어떤 선행 작업 때문에 막혀 있는지를 코드와
분리해 명확하게 유지하는 것이다.

현재 주의 단일 소스는
[current-week-work-graph.json](./docs/meetings/current-week-work-graph.json)이다.

## 1. 작업을 시작하기 전에

### 1.1 자신의 작업을 조회한다

```bash
pnpm work:me -- "나는 정행이야"
pnpm work:me -- Sean
pnpm work:me -- Amy
pnpm work:me -- --all
```

조회 결과에서 다음을 확인한다.

- 내가 담당자인 작업 ID와 이번 주 다음 행동
- 단일 리뷰어와 그 사람을 선택한 이유
- 아직 끝나지 않은 선행 작업
- 내가 리뷰해야 할 다른 사람의 작업
- 팀 확인이 필요한 미배정 작업

### 1.2 그래프에 없는 일을 임의로 시작하지 않는다

- 담당자가 확실하지 않은 일은 `unassigned`로 남긴다.
- 본인이 관심 있거나 관련 코드를 잘 안다는 이유만으로 `ownerId`를 자기 이름으로 바꾸지 않는다.
- 회의 사이에 긴급 작업이 생기면 먼저 미배정 작업으로 기록하고, 팀 책임자가 담당자 한 명과 리뷰어
  한 명을 확인한 뒤 시작한다.
- 원인 조사만 필요한 경우에도 조사 결과, 범위, 후속 구현 담당자를 구분한다. 조사가 자동으로 구현
  소유권을 주지는 않는다.

### 1.3 시작 조건을 확인한다

구현을 시작하려면 다음이 필요하다.

- 작업 ID가 있다.
- `ownerId`가 본인으로 확인되어 있다.
- 본인과 다른 리뷰어가 정확히 한 명 있다.
- `reviewerReason`에 그 사람이 무엇을 판단할지 설명되어 있다.
- 선행 작업과 현재 차단 상태를 확인했다.
- 완료 결과, 다음 행동, 필요한 검증 증거가 적혀 있다.

새 작업은 가능하면 리뷰어를 `confirmed`로 바꾼 뒤 구현을 시작한다. 현재처럼 이미 진행 중인 작업의
리뷰어가 `proposed`라면 구현은 이어갈 수 있지만, 가능한 한 빨리 확인하고 PR 리뷰 요청 전에는 반드시
`confirmed`로 바꾼다. 확인 전에는 구현 결과를 완료 처리하지 않는다.

## 2. 브랜치와 구현

1. `staging` 최신 상태에서 작은 feature 브랜치를 만든다.
2. 하나의 브랜치는 하나의 작업 ID 또는 독립적으로 리뷰 가능한 작은 slice만 다룬다.
3. 구현 시작 시 작업 상태를 `planned`에서 `in_progress`로 바꾼다.
4. 새 의존 작업이 발견되면 코드에 우회 처리를 숨기기 전에 `dependsOn`에 연결한다.
5. 다른 사람 작업과 같은 파일·migration·API 계약을 바꿀 가능성이 있으면 그래프와 작업 카드에
   충돌 가능성과 병합 순서를 기록한다.
6. 현재 작업과 무관한 포맷, 리팩터링, 문서 이동을 섞지 않는다.

개발 기간에는 `main`에 직접 push하거나 병합하지 않는다. feature는 `staging`에서 통합하고,
8월 22일 직전 승인된 릴리스 후보만 `main`으로 승격한다.

## 3. 구현 중 그래프 갱신

그래프는 회의 직후 한 번 만들고 버리는 계획표가 아니다. 다음 사건이 생기면 같은 변경 또는 작업
카드에서 함께 갱신한다.

| 사건 | 갱신할 내용 |
|---|---|
| 작업 시작 | `status: in_progress` |
| 새로운 선행 조건 발견 | `dependsOn`과 차단 이유 |
| 외부 결정 대기 | `status: blocked`, `nextAction` |
| 담당자 변경 | 새 `ownerId`, 변경 이유, 팀 확인 |
| 리뷰어 변경 | 새 `reviewerId`, `reviewerReason`, 변경 이유와 확인 상태 |
| 범위 분리 | 새 작업 ID와 두 작업 사이의 의존성 |
| 검증 완료 | 실제 관찰한 `evidence` |
| 작업 완료 | 완료 조건과 리뷰 확인 후 `status: done` |

담당자나 리뷰어를 지우면서 작업 자체를 삭제하지 않는다. 결정되지 않은 정보는 미배정 상태로 남겨
다음 회의에서 보이게 한다.

## 4. 리뷰 요청

리뷰 요청은 주간 그래프에 기록된 한 사람에게만 보낸다. 보호 영역이라고 리뷰어를 추가하지 않고,
그 위험을 판단할 수 있는 한 사람을 선택한다.

PR에는 코드 파일 설명보다 다음 순서의 브리핑을 먼저 쓴다.

1. 사용자가 새로 할 수 있는 일
2. 변경 전과 변경 후의 흐름
3. 돈, 권한, 데이터, 외부 서비스에 대한 가장 위험한 실패
4. 실패를 막거나 발견하는 장치
5. 자동 검사, Playwright, staging에서 실제 관찰한 증거
6. 의도적으로 포함하지 않은 범위
7. 리뷰어가 결정해야 할 구체적인 질문

리뷰어는 모든 코드를 처음부터 다시 작성하듯 읽지 않는다. 먼저 결과와 위험, 증거를 판단하고 그
판단을 검증하는 코드 경계만 깊게 확인한다.

## 5. 필수 검증

변경 영역에 맞는 검증을 실행하고 결과를 PR에 기록한다.

- 품질·타입·단위/통합 테스트·빌드
- 사용자 화면 변경의 Playwright 흐름
- production 의존성 변경의 `pnpm audit:prod`
- DB 변경의 migration·schema·invariant 검사
- 결제·배송·정산·분쟁 변경의 staging 전체 흐름과 중복·재시도 검사

자동 `CI`는 품질, Playwright smoke, production dependency audit를 모두 통과해야 한다. 공유 DB
migration은 같은 SHA의 최종 `CI`가 성공한 뒤에만 실행한다.

## 6. 완료와 인계

다음이 모두 충족되어야 작업을 `done`으로 바꾼다.

- 그래프의 결과와 완료 조건을 만족했다.
- 단일 리뷰어가 요청받은 판단을 끝냈다.
- 필요한 자동·수동 검증 증거가 있다.
- 후속 작업의 차단 여부와 인터페이스가 갱신되었다.
- 남은 문제를 완료한 것처럼 숨기지 않고 새 작업 또는 미배정 작업으로 남겼다.

작업을 다른 사람에게 넘길 때는 “어떤 파일을 수정했다”보다 다음을 전달한다.

- 현재 사용자 흐름과 관찰 결과
- 완료된 조건과 남은 조건
- 실패 사례와 재현 방법
- 선행·후속 작업
- 다음 사람이 내려야 할 결정

## 7. 토요일 회의

회의는 보통 토요일 20:00 또는 20:30에 진행한다. 정확한 시작 시각과 시간대는 매주 공지에서
확정한다.

회의 종료 전 각 구성원은 자기 이름으로 조회한 결과를 읽고 다음을 확인한다.

- 담당 작업이 맞는가?
- 작업마다 리뷰어가 정확히 한 명인가?
- 리뷰어가 실제로 해당 판단을 할 수 있는가?
- 누락된 의존성이나 충돌이 있는가?
- 미배정 작업 중 이번 주에 담당자를 정해야 할 것은 무엇인가?

상세 구조와 상태 정의는 [주간 작업 그래프 사용법](./docs/meetings/WORK_GRAPH.md), 리뷰·릴리스 기준은
[개발 그래프와 리뷰 운영](./docs/mvp/DEVELOPMENT_GRAPH_AND_REVIEW.md)을 따른다.
