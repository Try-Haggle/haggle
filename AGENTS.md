# Haggle Agent Instructions

이 파일은 저장소 전체에 적용된다. 코드나 문서를 변경하는 모든 개발 에이전트는 먼저
[CONTRIBUTING.md](./CONTRIBUTING.md)를 읽고 주간 작업 그래프를 따른다.

## 작업 전 필수 확인

1. 사용자의 이름 또는 별칭과 요청을 `docs/meetings/current-week-work-graph.json`의 작업 ID에 연결한다.
2. `pnpm work:me -- "<이름 또는 별칭>"`으로 담당 작업, 리뷰 작업, 의존성, 미배정 항목을 확인한다.
3. 그래프에 없는 작업이나 담당자가 불명확한 작업은 사람에게 임의 배정하지 않는다.
4. 구현 작업에는 담당자 한 명과 그 사람과 다른 단일 리뷰어 한 명이 있어야 한다.
5. 리뷰어가 `proposed` 상태라면 가능한 한 빨리 확인하고, `confirmed` 전에는 완료 처리나 리뷰 요청으로
   진행하지 않는다. 이미 진행 중인 구현은 담당자와 의존성이 확실하면 계속할 수 있다.

## 변경 중 필수 규칙

- 하나의 브랜치와 PR은 하나의 작업 ID 또는 독립적으로 리뷰 가능한 slice를 다룬다.
- 새 선행 조건, 차단, 담당자·리뷰어 변경, 범위 분리는 주간 그래프와 작업 카드에 반영한다.
- 사용자의 기존 변경과 다른 사람의 작업을 보존하고, 관련 없는 수정은 섞지 않는다.
- 리뷰 설명은 코드 줄 목록보다 사용자 결과, 변경 흐름, 위험, 보호장치, 검증 증거, 판단 질문을 먼저
  제시한다.
- 품질·테스트·Playwright·production dependency audit 등 변경 영역에 맞는 검증을 실행한다.
- 공유 DB migration은 같은 SHA의 최종 CI 성공 전에 실행하지 않는다.
- 사용자의 명시적 요청 없이 commit, push, merge, PR 생성, staging/production 배포를 하지 않는다.
- 개발 기간에는 `main`을 변경하지 않는다.

## 완료 조건

- 단일 리뷰어의 판단과 필요한 검증 증거가 있어야 `done`으로 표시한다.
- 남은 문제는 숨기거나 임의 배정하지 않고 새 작업 또는 `unassigned`로 남긴다.
- 인계 시 파일 목록만 전달하지 말고 사용자 결과, 남은 조건, 실패 재현, 의존성, 다음 결정을 설명한다.
